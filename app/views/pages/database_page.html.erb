<% page_title @page.title %>
<article class="wrap measure">
  <h1 class="text-3xl"><%= @page.title %></h1>
  <div class="prose mt-6 text-ink-600">
    <%= simple_format @page.body %>
  </div>
</article>
