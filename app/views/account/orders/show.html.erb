<% page_title "Order #{@order.number}" %>

<section class="wrap measure">
  <p class="meta uppercase"><%= link_to "Orders", account_orders_path, class: "underline" %></p>
  <h1 class="mt-3 text-3xl"><%= @order.number %></h1>

  <div class="mt-4 flex flex-wrap items-center gap-4">
    <%= status_chip @order.status, tone: (@order.paid? ? :ok : :warn) %>
    <span class="meta">Placed <%= @order.placed_at.strftime("%-d %b %Y, %H:%M UTC") %></span>
  </div>

  <dl class="mt-8 space-y-3 border-y border-paper-100 py-5 text-sm">
    <div class="flex justify-between gap-4">
      <dt class="text-ink-600">Plan</dt>
      <dd><%= @order.plan.name %> (<%= @order.plan.billed_as %>)</dd>
    </div>
    <div class="flex justify-between gap-4">
      <dt class="text-ink-600">Amount</dt>
      <dd><%= formatted_price(@order.amount_cents, @order.currency) %></dd>
    </div>
    <% if @order.paid_at %>
      <div class="flex justify-between gap-4">
        <dt class="text-ink-600">Paid</dt>
        <dd><%= @order.paid_at.strftime("%-d %b %Y") %></dd>
      </div>
    <% end %>
  </dl>

  <p class="meta mt-6"><%= link_to "View the public order page", order_path(@order), class: "underline" %></p>
</section>
